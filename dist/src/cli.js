#!/usr/bin/env node
// cli.ts
import { renderInvoiceForBackend } from './build-html';
// Optionally, handle command-line arguments with a package like yargs or commander
// Execute the main function from build-html.ts
renderInvoiceForBackend();
